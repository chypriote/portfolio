nav.navigation
	.main-bar
		.logo
			a(href="#home")
				span.name nicolas
					span.surname temenides
		a.mobile.open-menu MENU
			i.fa.fa-bars
		.menu
			each link in nav
				a(href="#{link.url}") #{link.title}
	.sub-bar
		.menu.mobile
			each link in nav
				a(href="#{link.url}") #{link.title}