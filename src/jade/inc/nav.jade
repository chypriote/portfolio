- var nav = [{title:'Accueil', url:'#home'}, {title:'Compétences', url:'#skills'},{title:'Projets', url:'#portfolio'}];

nav.navigation
	.container
		.row
			.col-xs-12
				.pull-left
					a(href="#header")
						span.name Text
							span.surname te
				.pull-right
					img.visible-xs.open-menu(src="assets/images/open-menu.png" alt="Open menu")
				.pull-right
					ul.hidden-xs
						each link in nav
							li: a(href="#{link.url}") #{link.title}
				.mobile-menu
					ul
						each link in nav
							li: a(href="#{link.url}") #{link.title}
						